import React from 'react';

// TypeScript users only add this code

const CreativeProcessSection: React.FC = () => {
	return <div>CreativeProcessSection</div>;
};

export default CreativeProcessSection;
